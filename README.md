## Pipe file to gtk file dialog
This application opens a file dialog.
When the user saves it tries to read stdin and saves to where the user chose.

I use it to pipe and save screenshots via keybinds.
